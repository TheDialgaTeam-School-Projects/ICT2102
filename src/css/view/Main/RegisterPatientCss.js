import {StyleSheet} from 'react-native';
import {Color} from '../../Color';

/**
 * Stylesheet containing register patient styles.
 * @type {{registerPatientContainer: object}}
 */
export const RegisterPatientCss = StyleSheet.create({
  registerPatientContainer: {
    flex: 1,
    justifyContent: 'center',
    backgroundColor: Color.bodyBackgroundColor,
  },
});
