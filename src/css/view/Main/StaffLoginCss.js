import {StyleSheet} from 'react-native';
import {Color} from '../../Color';

/**
 * Stylesheet containing staff login styles.
 * @type {{staffLoginContainer: object}}
 */
export const StaffLoginCss = StyleSheet.create({
  staffLoginContainer: {
    flex: 1,
    justifyContent: 'center',
    backgroundColor: Color.bodyBackgroundColor,
  },
});
